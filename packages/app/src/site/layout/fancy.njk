---
layout: base-body
banner:
  image: /img/banner-1.jpeg
  alt: "آکادمی کوچینگ نجات"
  aspect: aspect-square
  gap: pt-[93%]
---

<div class="fixed top-0 left-0 right-0 z-below">
  <img class="w-full h-auto {{ banner.aspect }}" src="{{ banner.image }}" alt="{{ banner.alt }}" />
</div>

<div class="flex flex-col {{ banner.gap }} min-h-screen-safe">
  {{ content | safe }}
</div>
